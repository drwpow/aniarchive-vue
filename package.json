{
  "name": "aniarchive-www",
  "author": {
    "email": "drew@pow.rs",
    "name": "Drew Powers"
  },
  "scripts": {
    "build": "NODE_ENV=production svelvet",
    "build:gql": "mkdir -p dist && node scripts/graphql",
    "dev": "npm run build:gql && svelvet"
  },
  "dependencies": {
    "gqlmin": "0.0.1",
    "rollup-plugin-svelte": "^5.1.1",
    "svelte": "^3.18.2",
    "svelte-routing": "^1.4.0",
    "svelvet": "^0.3.1"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "cosmiconfig": "^6.0.0",
    "deepmerge": "^4.2.2",
    "jsonschema": "^1.2.5",
    "prettier": "^1.19.1"
  }
}
