query allFilms {
  films(orderBy: { releaseYear: desc }) {
    directors {
      alias
      firstName
      lastName
    }
    id
    title
    titleEN
    releaseYear
    image {
      name
      url
    }
    studio {
      name
    }
  }
}
